name: Specs

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [master]
  workflow_dispatch:
jobs:
  specs:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Crystalline embeds the compiler it is built with, so the build version
        # decides which language user code is analyzed against. There is no point
        # testing against a version no release would ever be built with.
        crystal: [1.21.0]
    steps:
      - name: Download source
        uses: actions/checkout@v6.0.2
      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1.9.2
        with:
          crystal: ${{ matrix.crystal }}
      - name: Install dependencies
        run: shards install --ignore-crystal-version
      - name: Build executable
        run: shards build crystalline --no-debug -Dpreview_mt
      - name: Run tests
        run: crystal spec
